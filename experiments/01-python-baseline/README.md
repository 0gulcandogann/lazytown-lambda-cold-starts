# 01 — Python baseline

Tiny Python 3.13 Lambda used as a simple cold/warm baseline.

**Configuration:** ARM64, 512 MB, JSON response.

```bash
sam build
sam deploy --guided
```

Payload: `payload.json`.
