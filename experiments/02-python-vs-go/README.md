# 02 — Python vs Go

A tiny equivalent response implemented in Python and Go, both on ARM64 at 512 MB.

Build the Go binary locally with:

```bash
cd go
./build-local.sh
```

For SAM:

```bash
sam build --use-container
sam deploy --guided
```
