# 05 — Memory tuning

Runs the same CPU-bound SHA-256 handler at 128, 512, 1024, and 1769 MB on ARM64.

This experiment is about latency behavior. The checked-in aggregate billed-duration field should not be used as a full cost analysis.
